<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ntropy.work.mapper.SettlementMapper">

    <resultMap id="settlementResultMap" type="com.ntropy.work.domain.entity.Settlement">
        <id property="settlementId" column="settlement_id"/>
        <result property="userId" column="user_id"/>
        <result property="status" column="status"/>
        <result property="jobId" column="job_id"/>
        <result property="periodStart" column="period_start"/>
        <result property="periodEnd" column="period_end"/>
        <result property="depositDate" column="deposit_date"/>
        <result property="expectedAmount" column="expected_amount"/>
        <result property="actualAmount" column="actual_amount"/>
        <result property="transactionCount" column="transaction_count"/>
        <result property="accountTransactionId" column="account_transaction_id"/>
        <result property="matchedAt" column="matched_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.ntropy.work.domain.entity.Settlement"
            useGeneratedKeys="true" keyProperty="settlementId">
        INSERT INTO SETTLEMENT
            (user_id, status, job_id, period_start, period_end, deposit_date, expected_amount, actual_amount,
             transaction_count, account_transaction_id, matched_at)
        VALUES
            (#{userId}, #{status}, #{jobId}, #{periodStart}, #{periodEnd}, #{depositDate}, #{expectedAmount},
             #{actualAmount}, #{transactionCount}, #{accountTransactionId}, #{matchedAt})
    </insert>

    <select id="existsByAccountTransactionId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM SETTLEMENT
        WHERE account_transaction_id = #{accountTransactionId}
    </select>

    <select id="existsByUserIdAndStatusAndPeriod" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM SETTLEMENT
        WHERE user_id = #{userId}
          AND status = #{status}
          AND period_start = #{periodStart}
          AND period_end = #{periodEnd}
    </select>

    <select id="findByUserIdAndDepositDateRange" resultMap="settlementResultMap">
        SELECT *
        FROM SETTLEMENT
        WHERE user_id = #{userId}
          AND deposit_date BETWEEN #{startDate} AND #{endDate}
    </select>

    <select id="findByUserIdInAndDepositDateRange" resultMap="settlementResultMap">
        SELECT *
        FROM SETTLEMENT
        WHERE user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        AND deposit_date BETWEEN #{startDate} AND #{endDate}
    </select>

    <select id="findByJobIdInAndDepositDateRangeAndStatus" resultMap="settlementResultMap">
        SELECT *
        FROM SETTLEMENT
        WHERE job_id IN
        <foreach collection="jobIds" item="jobId" open="(" separator="," close=")">
            #{jobId}
        </foreach>
        AND deposit_date BETWEEN #{startDate} AND #{endDate}
        AND status = #{status}
    </select>

</mapper>
