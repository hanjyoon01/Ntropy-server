<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ntropy.account.mapper.AccountLifecycleMapper">

    <update id="deactivateByIdAndUserId">
        UPDATE ACCOUNT
        SET status = 'INACTIVE',
            deactivated_at = COALESCE(deactivated_at, CURRENT_TIMESTAMP)
        WHERE account_id = #{id}
          AND user_id = #{userId}
    </update>

    <update id="activateByIdAndUserId">
        UPDATE ACCOUNT
        SET status = 'ACTIVE',
            deactivated_at = NULL
        WHERE account_id = #{id}
          AND user_id = #{userId}
    </update>

</mapper>
